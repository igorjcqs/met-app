import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  errorContainer: {
    backgroundColor: "rgba(107, 0, 0, 1)",
    width: 95,
    height: 30,
    borderRadius: 20,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  text: {
    fontSize: 15,
    color: "#FF0000",
  },
});
